export enum EThemeProperty {
  color = 'color',
  spacing = 'spacing',
}

export enum ESize {
  xs = 0,
  sm = 1,
  md = 2,
  lg = 3,
  xl = 4,
  xxl = 5,
}

export enum EVariant {
  default = 'default',
  primary = 'primary',
  secondary = 'secondary',
  success = 'success',
  warning = 'warning',
  danger = 'danger',
}
